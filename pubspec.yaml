name: varranger_controller
description: Controller App for vArranger2
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_midi_command: ^0.4.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true